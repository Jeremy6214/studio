
"use client";

// Este componente ya no es necesario en su forma actual, ya que dependía de Firebase.
// Se puede dejar como un placeholder o eliminarlo completamente.
// Por ahora, lo dejaré vacío para evitar errores de importación si algún
// archivo aún lo referencia (aunque no debería).

export function SettingsForm() {
  return (
    <div>
      <p>El formulario de configuración de usuario ha sido desactivado.</p>
      <p>Esta funcionalidad requería inicio de sesión.</p>
    </div>
  );
}
